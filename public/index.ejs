<!DOCTYPE html>
<html lang="cn">
<head>
  <meta charset="utf-8"/>
  <link rel="icon" href="%PUBLIC_URL%/favicon.ico"/>
  <meta name="theme-color" content="#000000"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="description" content="Web site created using create-react-app"/>
  <link rel="apple-touch-icon" href="%PUBLIC_URL%/logo192.png"/>
  <link rel="manifest" href="%PUBLIC_URL%/manifest.json"/>
  <title>React基础项目</title>
  <script src="%PUBLIC_URL%/index.js"></script>
  <script>window.init(<%= JSON.stringify(data) %>)</script>
</head>
<body>
  <div id="root"></div>
  <% if (data.isEnvProduction) { %>
    <% if (data.prerenderSpaPlugin){ %>
      <script>
        (function (prerenderRoutes) {
          if (!prerenderRoutes.includes(window.location.pathname)) {
            document.getElementById('root').innerHTML = ''
          }
        })(<%= JSON.stringify(data.prerenderRoutes) %>)
      </script>
    <% } %>
  <% }else { %>

  <% } %>
</body>
</html>
